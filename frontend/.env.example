# Frontend Environment Variables
# Vite requires VITE_ prefix for env vars to be exposed to the browser

# Admin Secret for x402 bypass (must match backend ADMIN_SECRET)
VITE_ADMIN_SECRET="your-admin-secret-here"
